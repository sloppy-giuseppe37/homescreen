[project]
name = "faikout"
version = "0.2.0"
description = "Pythonic MQTT API for Faikout (Daikin AC) devices"
requires-python = ">=3.10"
dependencies = [
    "gmqtt>=0.7.0",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = ["pytest"]
